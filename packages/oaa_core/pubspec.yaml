name: oaa_core
description: >-
  The Open Audio Analyzer domain model: what a metric is, what a delivery target is, and how a
  meter layout is described. Pure Dart — no Flutter, no dart:ffi, no I/O.
version: 0.7.0
publish_to: 'none'

environment:
  sdk: ^3.12.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.28.0

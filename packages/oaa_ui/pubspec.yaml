name: oaa_ui
description: >-
  The Open Audio Analyzer design system: spacing, colour and type tokens, and the widget
  primitives every meter module is assembled from.
version: 0.10.1
publish_to: 'none'

environment:
  sdk: ^3.12.2
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  oaa_core:
    path: ../oaa_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

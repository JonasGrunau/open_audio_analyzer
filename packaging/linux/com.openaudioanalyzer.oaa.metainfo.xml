<?xml version="1.0" encoding="UTF-8"?>
<!--
  SPDX-License-Identifier: GPL-3.0-or-later

  AppStream metadata. Required by flatpak (flatpak-builder validates it) and
  read by GNOME Software, KDE Discover and AppImage catalogues.

  `project_license` is the application's licence, not the repository's. Open Audio Analyzer is
  several licences at once — the engine is MIT, the plugin is AGPL — and what
  ships in this bundle is the GPL-3.0-or-later application linked against the
  MIT engine. GPL-3.0-or-later is therefore the correct and honest answer for
  the binary a user installs from here; see README.md for the full split.
-->
<component type="desktop-application">
  <id>com.openaudioanalyzer.oaa</id>
  <launchable type="desktop-id">com.openaudioanalyzer.oaa.desktop</launchable>

  <name>Open Audio Analyzer</name>
  <summary>Loudness and spectrum metering for mixing, mastering and delivery</summary>

  <metadata_license>CC0-1.0</metadata_license>
  <project_license>GPL-3.0-or-later</project_license>

  <developer id="com.openaudioanalyzer">
    <name>The Open Audio Analyzer contributors</name>
  </developer>

  <description>
    <p>
      Open Audio Analyzer is a free and open-source loudness and spectrum analyzer: an
      arrangeable canvas of meter modules driven by presets, delivery targets
      and skins.
    </p>
    <p>
      Loudness follows EBU R 128 and ITU-R BS.1770-4 — momentary, short-term and
      integrated LUFS, loudness range, and true peak by four-times oversampling —
      and is held against the EBU Tech 3341 and 3342 conformance vectors on every
      build. What Open Audio Analyzer has not measured it does not draw: an unavailable quantity
      is a dash, never a zero.
    </p>
    <p>Thirteen modules:</p>
    <ul>
      <li>Number Box, LUFS Meter, Digital Meter and Super Meter</li>
      <li>Alert Meter, Validator and Histogram</li>
      <li>VU Meter with a real second-order movement</li>
      <li>Spectrum Analyzer, Spectrogram, Phase Scope and Stereo Cloud</li>
    </ul>
    <p>
      Files can be analysed offline against a delivery target, from the
      application or from the bundled command-line analyser. A tablet on the
      same network can mirror the canvas as a second display.
    </p>
  </description>

  <categories>
    <category>AudioVideo</category>
    <category>Audio</category>
  </categories>

  <url type="homepage">https://github.com/JonasGrunau/open_audio_analyzer</url>
  <url type="bugtracker">https://github.com/JonasGrunau/open_audio_analyzer/issues</url>
  <url type="vcs-browser">https://github.com/JonasGrunau/open_audio_analyzer</url>

  <supports>
    <control>pointing</control>
    <control>keyboard</control>
    <control>touch</control>
  </supports>

  <!-- A canvas of thirteen meters does not fit on a phone. -->
  <requires>
    <display_length compare="ge">768</display_length>
  </requires>

  <content_rating type="oars-1.1"/>

  <releases>
    <release version="0.7.0" date="2026-08-22">
      <description>
        <p>
          The Linux download that carries the VST3 is a tarball: unpack it and
          run ./install.sh, which needs no root and writes under your home
          directory unless you ask it for --system. It is its own uninstaller
          too. This flatpak still installs the application alone, and that is a
          property of the format rather than something left undone — a plugin
          built against the sandbox's libraries cannot be loaded by a DAW
          running on the host's. The Linux VST3 is now built against the same
          glibc as everything shipped beside it, where being built against a
          newer one made it absent from an older distribution's plugin browser
          with nothing logged. A display can also be paired by camera now: the
          desk shows a QR code and a tablet reads it, which is the route that
          works on a network that blocks multicast.
        </p>
      </description>
    </release>
    <release version="0.6.0" date="2026-08-22">
      <description>
        <p>
          The application identifier is now com.openaudioanalyzer.oaa. On Linux
          that is also the flatpak application id, so this release installs
          beside an earlier one rather than upgrading it; remove the old
          dev.openaudioanalyzer.oaa if you do not want both. Downloaded macOS
          builds no longer need a quarantine flag stripped by hand, because the
          plugin bundles and the disk image are notarised. The VST3 and Audio
          Unit are universal and load on macOS 11 and later, where every
          previous release shipped a bundle built only for the machine that
          built it.
        </p>
      </description>
    </release>
    <release version="0.5.0" date="2026-08-22">
      <description>
        <p>
          The canvas no longer slows down the longer a spectrogram is on screen:
          its history is now kept as pixels and drawn as one image, at a fixed
          cost whatever the signal does. On a remote display the tab control sits
          beside the name of the machine being watched instead of stranded in the
          middle of the link bar. On macOS, double-clicking the status bar zooms
          the window again, obeying the system's title bar setting. The iOS and
          Android home screen label is now OAA, which a tablet has room to draw.
        </p>
      </description>
    </release>
    <release version="0.4.1" date="2026-08-22">
      <description>
        <p>
          The macOS VST3 and Audio Unit now carry a valid code signature. Every
          previous release shipped one that did not verify, which on Apple
          silicon is enough for a DAW to leave the plugin out of its browser
          without saying why. The documentation site now says which folder each
          format has to be copied into, and what a downloaded copy needs before
          macOS will load it.
        </p>
      </description>
    </release>
    <release version="0.4.0" date="2026-08-21">
      <description>
        <p>
          Metering your Mac's own output no longer needs a loopback driver: on
          macOS 14.2 and later, System Output is a Core Audio process tap with
          nothing to install and nothing to reroute. An Android tablet finds
          hosts on the network by itself and remembers its layout, its skin and
          the host it was last attached to. The wire protocol is at version 3,
          and a display now accepts any version it knows rather than only its
          own, so a plugin left in a DAW's folder keeps working across an
          upgrade.
        </p>
      </description>
    </release>
    <release version="0.3.0" date="2026-08-21">
      <description>
        <p>
          A DAW's playhead now reaches a tablet, and both screens show the
          host's transport. The desktop accepts the plugin's connection at
          last, so inserting a VST3 or an AU meters what the DAW is playing.
          Two readings are corrected and warrant a re-measure: the clip
          indicator catches clipping it was missing, and crest is taken over
          the audio rather than off the meters' ballistics.
        </p>
      </description>
    </release>
    <release version="0.2.0" date="2026-08-19">
      <description>
        <p>
          Thirteen modules that all measure something. Loudness, true peak and
          the spectrum are held against the EBU R128 and BS.2217 reference
          vectors in CI. Layouts, delivery targets and skins persist; files are
          analysed offline; a tablet mirrors the canvas over the network, and a
          headless VST3 or AU plugin streams a DAW's audio to the desk.
        </p>
      </description>
    </release>
    <release version="0.1.0" date="2026-08-15">
      <description>
        <p>First release.</p>
      </description>
    </release>
  </releases>
</component>

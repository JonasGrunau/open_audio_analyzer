# Open Audio Analyzer — agent notes

The rules for this repository live in **[CLAUDE.md](CLAUDE.md)**, and the design
reasoning lives in **[README.md](README.md)**. They are not duplicated here,
because two copies of a rule is one copy of a rule and one copy of a lie.

Each directory carries its own `AGENTS.md` describing what belongs in it. Start
with `CLAUDE.md`, then read the one for wherever you are working.

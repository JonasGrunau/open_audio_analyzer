{
  "name": "open-audio-analyzer-website",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "measure": "node scripts/measure.mjs",
    "dev": "npm run measure && astro dev",
    "build": "npm run measure && astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy",
    "og": "'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome' --headless=new --disable-gpu --hide-scrollbars --window-size=1200,630 --virtual-time-budget=6000 --screenshot=public/og.png \"file://$PWD/scripts/og.html\"",
    "modules": "node scripts/render-modules.mjs"
  },
  "devDependencies": {
    "astro": "^7.2.4",
    "wrangler": "^4.28.0"
  }
}
